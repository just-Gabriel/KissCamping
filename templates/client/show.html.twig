{% extends 'base.html.twig' %}

{% block title %}Client{% endblock %}

{% block body %}
    <h1>Client</h1>
    <div class="Form">
    <table class="table table-dark table-hover">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ client.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ client.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ client.prenom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ client.email }}</td>
            </tr>
            <tr>
                <th>NbPersonne</th>
                <td>{{ client.nbPersonne }}</td>
            </tr>
            <tr>
                <th>NbJourSurSite</th>
                <td>{{ client.nbJourSurSite }}</td>
            </tr>
            <tr>
                <th>Avis</th>
                <td>{{ client.avis }}</td>
            </tr>
        </tbody>
    </table>
    </div>

    <button  class="btn btn-light"><a href="{{ path('app_client_index') }}">retour à la liste</a></button>

    <button  class="btn btn-success"><a href="{{ path('app_client_edit', {'id': client.id}) }}">modifier</a></button>

    {{ include('client/_delete_form.html.twig') }}
{% endblock %}
