{% extends 'base.html.twig' %}

{% block title %}Logement{% endblock %}

{% block body %}
    <h1>Logement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ logement.id }}</td>
            </tr>
            <tr>
                <th>NbPlace</th>
                <td>{{ logement.nbPlace }}</td>
            </tr>
            <tr>
                <th>Emplacement</th>
                <td>{{ logement.emplacement }}</td>
            </tr>
        </tbody>
    </table>

    <button class="btn btn-outline-light me-5" type="button"><a href="{{ path('app_logement_index') }}">back to list</a></button>

    <button class="btn btn-outline-light me-5" type="button"><a href="{{ path('app_logement_edit', {'id': logement.id}) }}">edit</a></button>

    {{ include('logement/_delete_form.html.twig') }}
{% endblock %}
